# ── Web Framework ─────────────────────────────────────
fastapi==0.128.0
uvicorn==0.40.0

# ── Database ───────────────────────────────────────────
sqlalchemy==2.0.45
aiosqlite==0.22.1            # Async SQLite driver (development)
# asyncpg==0.30.0            # Uncomment for PostgreSQL (production)
alembic==1.18.3              # Database migrations (Phase 3)

# ── Data Validation ────────────────────────────────────
pydantic==2.12.5
pydantic-settings==2.12.0
email-validator==2.3.0

# ── Authentication & Security ──────────────────────────
passlib[bcrypt]==1.7.4       # Password hashing
bcrypt==4.0.1                # Bcrypt backend
python-jose[cryptography]==3.5.0  # JWT encoding/decoding
python-multipart==0.0.21     # Form data support (OAuth2 forms)

# ── Environment ────────────────────────────────────────
python-dotenv==1.2.1
